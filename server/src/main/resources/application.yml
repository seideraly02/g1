server:
  port: ${PORT:3000}

spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/qadam_ent}
    username: ${DATABASE_USER:qadam}
    password: ${DATABASE_PASSWORD:qadam}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

qadam:
  frontend-origin: ${FRONTEND_ORIGIN:http://localhost:4173}
  auth-mode: ${AUTH_MODE:production}
  session-cookie-secure: ${SESSION_COOKIE_SECURE:true}
  telegram-gateway-url: ${TELEGRAM_GATEWAY_URL:}
  telegram-gateway-token: ${TELEGRAM_GATEWAY_TOKEN:}

springdoc:
  api-docs:
    enabled: ${SPRINGDOC_ENABLED:false}
  swagger-ui:
    enabled: ${SPRINGDOC_ENABLED:false}
    path: /swagger
